services:
  chronus:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: "chronus-studio"
    ports:
      - "127.0.0.1:8080:8080"
    volumes:
      - ./config.toml:/app/config.toml
      - ./storage:/app/storage
    restart: always
