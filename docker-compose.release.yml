services:
  chronus:
    image: ghcr.io/anudeep-reddy07/chronus_for_business:latest
    container_name: "chronus-studio"
    ports:
      - "127.0.0.1:8080:8080"
    volumes:
      - ./config.toml:/app/config.toml
      - ./storage:/app/storage
    restart: always
